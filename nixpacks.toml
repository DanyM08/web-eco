[phases.setup]
nixPkgs = ["php82", "php82Extensions.intl", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.mysqlnd", "php82Extensions.pdo_mysql", "composer"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
